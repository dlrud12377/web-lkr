<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace : 현재 Mapper XML이 어떤 Java 인터페이스와 연결되는지 지정 -->  
<mapper namespace="com.example.book2.dao.BookDAO">

	<!-- id : DAO 인터페이스의 메서드 이름과 연결
	resultType : SQL 실행해서 조회된 결과를 어떤 JAVA 객체에 담을 것인지 지정 -->
	<select id="findAll" resultType="BookVO">
		SELECT id, title, author, category, price, stock FROM books
	</select>
	
	<!-- id를 통한 단건 조회 -->
	<select id="findById" resultType="BookVO">
		SELECT id, title, author, category, price, stock FROM books WHERE id=#{id}
	</select>
	
	<!-- 도서 등록 -->
	<insert id="insert">
		INSERT INTO books(title, author, category, price, stock) values(#{title}, #{author}, #{category}, #{price}, #{stock})
	</insert>
	
	<!-- 도서 수정 -->
	<update id="update">
		UPDATE books
		SET 
			name = #{title},
			author = #{author}
			category = #{category},
			price = #{price},
			stock = #{stock}
		where id=#{id}
	</update>

	<!-- 도서 삭제 -->
	<delete id="delete">
		delete from books
		where id = #{id}
	</delete>
	
	<!-- 카테고리별 도서 조회 -->
	<select id="findByCategory" resultType="BookVO">
		SELECT id, title, author, category, price, stock FROM books where category LIKE '#{category}'
	</select>
	
	<!-- 특정 이름을 포함한 도서 제목 조회 -->
	<select id="findByTitleContaining" resultType="BookVO">
		SELECT id, title, author, category, price, stock FROM books where title LIKE CONCAT('%', #{title}, '%')
	</select>
	
	<!-- 가격이 30000원보다 비싼 책 조회 -->
	<select id="findByPriceGreaterThanOrderByPriceAsc" resultType="BookVO">
		SELECT id, title, author, category, price, stock FROM books where price >= #{price} ORDER BY price ASC
	</select>
	
	<!-- 책 개수 세기 -->
	<select id="count" resultType="int">
		SELECT count(*) FROM books
	</select>
	
	<!-- 저자에 따른 책 조회 -->
	<select id="findByAuthor" resultType="BookVO">
		SELECT * FROM books
		<if test="author != null">
			where author = #{author}
		</if>
	</select>
	
	<!-- 정렬 조건을 사용자가 선택하는 경우 -->
	<select id="findAllOrder" resultType="BookVO">
		select * from books
		<choose>
			<when test="sort == 'priceDesc'">
				ORDER BY price DESC
			</when>
			<when test="sort == 'priceAsc'">
				ORDER BY price ASC
			</when>
			<when test="sort == 'titleAsc'">
				ORDER BY title ASC
			</when>
			<otherwise>
				ORDER BY id DESC
			</otherwise>
		</choose>
	</select>
</mapper>